{
  "dependencies": {
    "@types/fs-extra": "^9.0.13",
    "ethers": "^5.7.2",
    "fs-extra": "^10.1.0",
    "solc": "^0.8.17",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base-path . -o . SimpleStorage.sol",
    "deploy": "npx tsc deploy.ts && node deploy.js "
  }
}
